# Telegram
BOT_TOKEN=replace-with-bot-token
BOT_DEV_ID=123456789
BOT_GROUP_ID=-1001234567890

# Emojis for statuses
EMOJI_NEW=🆕
EMOJI_IN_PROGRESS=🔵
EMOJI_RESOLVED=✅

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Encryption (Fernet key) - generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=replace-with-base64-key

# Reminders (seconds)
REMINDER_USER_INTERVAL=86400         # remind user if ticket unresolved for 1 day
REMINDER_OPERATOR_INTERVAL=43200     # remind operators for old tickets every 12 hours

# Localization
DEFAULT_LANG=ru

# Admin web
ADMIN_BIND=0.0.0.0
ADMIN_PORT=8080

# Backups
BACKUP_PATH=/backups
BACKUP_CRON="@daily"